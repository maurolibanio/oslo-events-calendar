name: Update Oslo Events Calendar

on:
  workflow_dispatch:

  schedule:
    # Every 12h: event data changes slowly, less pressure on jina.ai rate limit
    - cron: "7 */12 * * *"

permissions:
  contents: write

jobs:
  update-calendar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Download events
        env:
          JINA_API_KEY: ${{ secrets.JINA_API_KEY }}
        run: |
          python src/fetch_events.py

      - name: Generate ICS
        run: |
          python src/generate_ics.py

      - name: Create heartbeat
        run: |
          date -u > last-run.txt

      - name: Commit updates
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add docs/oslo-all.ics
          git add last-run.txt

          git commit -m "Update calendar" --allow-empty

          git push
